apiVersion: v1
kind: ReplicationController
metadata:
  name: webapp
  labels:
    app: ecoinfo
    tier: frontend
spec:
  replicas: 2
  # Pod Template
  template:
    metadata:
      labels:
        app: ecoinfo
        tier: frontend
    spec:
      containers:
      - name: server
        image: ecoinfo

